# Public canonical URL used by Next.js metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only credential used by POST /api/lesson-plan. Never prefix this with NEXT_PUBLIC_.
OPENAI_API_KEY=
